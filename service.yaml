---
apiVersion: v1
kind: Service
metadata:
  name: code-server-svc
spec:
  type: LoadBalancer
  ipFamilyPolicy: PreferDualStack 
  ipFamilies:
    - IPv4
    - IPv6
  ports:
    - port: 80
      targetPort: 8443
  selector:
    app: code-server